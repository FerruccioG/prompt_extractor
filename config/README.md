
# Configuration — Environment & Runtime Settings

## Overview

The `config/` directory centralizes **runtime configuration** for the Prompt Extractor pipeline.

Configuration is primarily provided via **environment variables** (typically loaded from a `.env` file at repo root).

---

## 🔐 Environment Variables

Required for the pipeline:

```text
EMAIL_ADDRESS
EMAIL_APP_PASSWORD
GMAIL_QUERY
