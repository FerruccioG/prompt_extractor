# Pipelines Layer — Orchestration (FROZEN BASELINE)

## Overview

The `pipelines/` directory contains the **orchestration logic** for the Prompt Extractor system.

The main pipeline:

```text
pipelines/run_pipeline_instagram.py
